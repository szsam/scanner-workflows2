name: "CodeQL: azure-rtos/netxduo"

on:
  schedule:
    - cron: '50 4 * * *'
  workflow_dispatch:
    inputs:
      repository:
        description: 'Repository to run CodeQL on. Format: owner/repo.'
        required: true
        type: string
        default: azure-rtos/netxduo
      ref:
        description: 'The branch, tag or SHA to checkout. Default: default branch.'
        required: false
        type: string

jobs:
  call_codeql_workflow:
    permissions:
      actions: read
      contents: read
      security-events: write
    uses: ./.github/workflows/codeql.yml
    with:
      repository: ${{ inputs.repository || 'azure-rtos/netxduo' }}
      ref: ${{ inputs.ref }}
      build_script: 'azure-rtos_netxduo.sh'
      install_arm_cross_compiler: true
